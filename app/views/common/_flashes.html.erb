<% if flash.any? %>
  <% user_faacing_flashes.each do |key, value| %>
     <div class="alert alert-dismissable alert-<%= flash_class(key) %>">
       <button class="close" date-dismiss="alert">×</button>
       <%= value %>
    </div>
  <% end %>
<% end %>
